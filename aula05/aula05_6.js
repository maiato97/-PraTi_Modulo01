let contador = 0;
let multiplicando = 5;

// <variáveis>; <condição>; <incremento>
for (contator = 0; contator <= 10; contator++){
        console.log(`${multiplicando} * ${contator} = `, multiplicando * contador)
    }